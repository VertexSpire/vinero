import { User } from '../interfaces/user.interface';

/**
 * Type representing fields in the User interface that can be used as strategies.
 * Adjust based on the actual fields in your User interface.
 */
export type UserStrategyField = keyof User;
